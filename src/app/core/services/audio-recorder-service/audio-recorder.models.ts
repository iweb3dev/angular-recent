export enum MediaRecordingState {
  Inactive = 'inactive',
  Recording = 'recording',
  Paused = 'paused',
}

export enum MediaRecorderEvents {
  Dataavailable = 'dataavailable',
  Error = 'error',
  Stop = 'stop',
}
