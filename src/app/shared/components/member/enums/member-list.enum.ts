export enum MemberListLimits {
  ScrollerLimit = 40,
  TopScrollLimit = 0,
  ThrottleLimit = 500,
  DebounceLimit = 1000,
  MobileViewLimit = 768,
  PhoneNumberLimit = 10,
  SearchInputValidLimit = 2,
}
