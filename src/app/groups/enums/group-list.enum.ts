export enum GroupListLimits {
  ScrollerLimit = 40,
  TopScrollLimit = 0,
  ThrottleLimit = 500,
  DebounceLimit = 1000,
  MobileViewLimit = 768,
  PhoneNumberLimit = 10,
  SearchInputValidLimit = 2,
}

export enum GroupFilters {
  SortFilter = 'sortFilter',
  OtherFilters = 'otherFilters',
}

export enum VirtualTableItemWidth {
  Small = 50,
  Large = 200,
  Medium = 100,
  ExtraSmall = 25,
  ExtraLarge = 400,
}
